import { StaticHeroCarousel } from "./StaticHeroCarousel";

/** Static storefront carousel; no API or database dependency. */
export function HeroSectionServer() {
    return <StaticHeroCarousel />;
}
